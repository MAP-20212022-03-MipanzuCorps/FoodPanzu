import 'package:flutter/material.dart';

class SSMRegScreen extends StatelessWidget {
  static String routeName = "/role_choice";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('SSM Registration'),
      ),
      body: Container(),
    );
  }
}
